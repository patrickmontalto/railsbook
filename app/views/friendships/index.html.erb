<h3>Friend Requests</h3>

<ul>
	<% current_user.received_friends.each do |rec_friend| %>
		<li>
			<%= link_to rec_friend.name, rec_friend %> | 
			<%= link_to "Accept", friendship_path(id: rec_friend), :method => "put" %>
		</li>
	<% end %>
</ul>